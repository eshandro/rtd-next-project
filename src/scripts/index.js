import React from "react";
import ReactDOM from "react-dom";

const Test = () => {
	return <div>Hello, React with Webpack and Webpack Dev Server!</div>
};

ReactDOM.render(<Test />, document.getElementById("react-app"))